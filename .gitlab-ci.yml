stages:
  - publish

deploy-artifacts:
  stage: publish
  image: gradle:7.2-jdk11
  before_script:
    - export BUILD_NUMBER=${CI_PIPELINE_IID}
  script:
    - 'gradle clean publish'
  only:
    - main